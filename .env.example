# Copy this file to .env.local or the project-specific environment file.
# Add only non-secret, repository-specific variable names here.
# Never commit credentials, tokens, private keys, or production values.

# Optional inputs for `bun run performance:live`.
SUBGRAPH_PERFORMANCE_URL=
SUBGRAPH_PERFORMANCE_TOKEN=
SUBGRAPH_CHAIN_RPC_URL=
